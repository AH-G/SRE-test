apiVersion: v1
kind: Service
metadata:
  name: sre-flask
  annotations:
    networking.gke.io/max-rate-per-endpoint: "20"
spec:
  selector:
    app: sre-flask
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
